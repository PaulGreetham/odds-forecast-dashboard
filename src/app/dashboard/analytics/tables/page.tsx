import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function AnalyticsTablesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Analytics - Tables</CardTitle>
      </CardHeader>
      <CardContent className="text-sm text-muted-foreground">
        Tables view coming next.
      </CardContent>
    </Card>
  );
}
